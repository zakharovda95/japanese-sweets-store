{
  "name": "japanese_sweets_store",
  "private": false,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean -y",
    "fontend:start:dev": "npm --prefix packages/frontend run serve",
    "backend:start:dev": "npm --prefix packages/strapi run develop"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
